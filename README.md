# simplest-react-project

React is a javascript library

- Used React CDNs to create a simple react app

Two CDNs used are:
  React (Core)
  ReactDom (web version of React)

Learned:
  Basic of React
  ReactDom
  React.createElement()
  ReactDom.createRoot()
  render method
